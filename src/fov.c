#include "fov.h"

void
rl_compute_fov(struct rl_map const* map,
               SDL_Point origin,
               int radius,
               bool* out)
{
    (void)map;
    (void)origin;
    (void)radius;
    (void)out;
}
